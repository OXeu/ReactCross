import { RouteProps } from "@thankrain/cross-core/dist/wrappers/navigation";

export function NavigationProvider(props: RouteProps) {
    const { element, ...rest } = props;
    return (
        <div/>
    );
}